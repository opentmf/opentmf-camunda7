opentmf:
  security:
    jwk-set-uri: classpath:jwk-set.json

spring:
  datasource:
    url: jdbc:tc:postgresql:16.4-alpine:///db
    hikari:
      schema: public

camunda.bpm.authorization:
  enabled: false

management:
  endpoints:
    access:
      default: none
